/**
 * Media queries for JavaScript (matchMedia). CSS uses the @custom-media rules in
 * src/styles/media.css; keep these values in sync with them.
 */
export const NAV_DESKTOP_QUERY = "(width >= 48rem)";
